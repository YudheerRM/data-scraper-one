selenium==4.10.0
beautifulsoup4==4.12.2
requests==2.31.0
fake-useragent==1.1.3
# Use --only-binary option to avoid building from source
openpyxl==3.1.2 --only-binary=:all:
# For JSON output instead of Excel when pandas fails
simplejson==3.19.2
